<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card ">
                <div class="card-body">
                    <form action="/roles/edit/{{rol.id}}" method="post">
                    <div class="form-group">
                        <input type="text" value="{{rol.name}}" class="form-control" name="name", placeholder="rol" autofocus>
                    </div>
                    <div class="form-group">
                        <input type="text"  value="{{rol.estado}}"name="estado"  class="form-control" placeholder="estado">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">
                            Editar
                        </button>
                    </div>
                    </form>
                </div>
            </div> 
        </div>
    </div>
</div>